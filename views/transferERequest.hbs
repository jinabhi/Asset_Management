<!DOCTYPE html>
<html lang="en">

<head>
  <title>Asset Management</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
  <style>
    body {
      margin: 0;
      font-family: "Lato", sans-serif;
    }

    .sidebar {
      margin: 0;
      padding: 0;
      width: 200px;
      background-color: #f1f1f1;
      position: fixed;
      height: 100%;
      overflow: auto;
    }

    .sidebar a {
      display: block;
      color: black;
      padding: 16px;
      text-decoration: none;
    }

    .sidebar a.active {
      background-color: rgb(0, 0, 0);
      color: white;
    }

    .sidebar a:hover:not(.active) {
      background-color: #555;
      color: white;
    }

    div.content {
      margin-left: 200px;
      padding: 1px 16px;
      height: 100%;
    }

    @media screen and (max-width: 700px) {
      .sidebar {
        width: 100%;
        height: auto;
        position: relative;
      }

      .sidebar a {
        float: left;
      }

      div.content {
        margin-left: 0;
      }
    }

    @media screen and (max-width: 400px) {
      .sidebar a {
        text-align: center;
        float: none;
      }
    }
  </style>

</head>

<body>
  <div class="sidebar">
    <a href="employeeHome">View Profile</a>
    <a href="employeeHome">Create Request</a>
    <a href="viewempReq">View My Requests</a>
    <a href="employeeHome">Change password</a>
    <a href="logout">Log Out</a>
  </div>

  <div class="content">

    <div class="container">
      <h2>Transfer your Asset</h2>
      {{#if msg}}
      <div class="alert alert-success alert-dismissible"><a href="#" class="close" data-dismiss="alert"
          aria-label="close">&times;</a> <strong>{{msg}}</strong> </div>
      {{/if}}
      <div class="jumbotron" style="width: 1000px; height: 100%">
        <div id="userTable">
          <table class='table table-hover'>
            <tr>
              <th>Asset ID</th>
              <th>Asset Name</th>
              <th>Transfer To</th>
            </tr>
            {{!--  --}}
            <tr>
              {{#each data}}<td>{{this.assetid}}</td>{{/each}}
              {{#each data}}<td>{{this.assetName}}</td>{{/each}}
              <td>
                <form action="transferOne" method="POST">
                  {{#each data}}<input type="hidden" value="{{this.assetid}}" name="trassid">{{/each}}
                  {{#each data}}<input type="hidden" value="{{this.assetName}}" name="trassname">{{/each}}
                  <select name="recepient" class="form-control" style="width: 100px">
                    {{#each dataone}}
                    <option value="{{empid}}">{{empid}}</option>
                    {{/each}}
                    <input type="submit" value="Create Transfer Request" class="btn btn-default">
                  </select>
                </form>
              </td>
            </tr>

          </table>
          <div class="col-sm-offset-2 col-sm-10">
            <a href="employeeHome" type="button" class="btn btn-default">Back</a>
          </div>
        </div>
      </div>
    </div>
  </div>

</body>

</html>