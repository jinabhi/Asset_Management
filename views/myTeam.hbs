<!DOCTYPE html>
<html lang="en">

<head>
  <title>Asset Management</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
  <style>
    body {
      margin: 0;
      font-family: "Lato", sans-serif;
    }

    .sidebar {
      margin: 0;
      padding: 0;
      width: 200px;
      background-color: #f1f1f1;
      position: fixed;
      height: 100%;
      overflow: auto;
    }

    .sidebar a {
      display: block;
      color: black;
      padding: 16px;
      text-decoration: none;
    }

    .sidebar a.active {
      background-color: rgb(0, 0, 0);
      color: white;
    }

    .sidebar a:hover:not(.active) {
      background-color: #555;
      color: white;
    }

    div.content {
      margin-left: 200px;
      padding: 1px 16px;
      height: 100%;
    }

    @media screen and (max-width: 700px) {
      .sidebar {
        width: 100%;
        height: auto;
        position: relative;
      }

      .sidebar a {
        float: left;
      }

      div.content {
        margin-left: 0;
      }
    }

    @media screen and (max-width: 400px) {
      .sidebar a {
        text-align: center;
        float: none;
      }
    }
  </style>

</head>

<body>
  <div class="sidebar">
    <a href="managerHome">View Profile</a>
    <a href="pendingReqMgr">View Pending Requests</a>
    <a href="managerHome">Request for Asset</a>
    <a href="viewmgrReq">View My Requests</a>
    <a href="managerHome">Change password</a>
    <a href="logout">Log Out</a>
  </div>

  <div class="content">

    <div class="container">
      <h2>View My Team</h2>
      {{#if msg}}
      <div class="alert alert-success alert-dismissible"><a href="#" class="close" data-dismiss="alert"
          aria-label="close">&times;</a> <strong>{{msg}}</strong> </div>
      {{/if}}
      <div class="jumbotron" style="width: 100%; height: 100%">

        <h3> My Team </h3>
        <div id="userTable">
          <table class='table table-hover'>
            <tr>
              <th>Name</th>
              <th>Employee ID</th>
              <th>Email</th>
              <th>Mobile Number</th>
              <th>Date of Joining</th>
            </tr>
            {{#each data}}
            <tr>
              <td>{{this.fname}} {{this.lname}}</td>
              <td>{{this.empid}}</td>
              <td>{{this.email}}</td>
              <td>{{this.mobNum}}</td>
              <td>{{this.doj}}</td>
            </tr>
            {{/each}}
          </table>
        </div>

        <h3> Team Requests </h3>

        <div id="userreqTable">
          <table class='table table-hover'>
            <tr>
              <th>Request ID</th>
              <th>Asset Name</th>
              <th>Rquest Date</th>
              <th>Request Status</th>
            </tr>
            {{#each dataone}}
            <tr>
              <td>{{this.reqid}} </td>
              <td>{{this.assetName}}</td>
              <td>{{this.reqDate}}</td>
              <td>{{this.reqStatus}}</td>
            </tr>
            {{/each}}
          </table>
        </div>


        {{!--
        <div id="userassetTable">
          <table class='table table-hover'>
            <tr>
              <th>Asset ID</th>
              <th>Asset Name</th>
              <th>Issue Date</th>
            </tr>
            {{#each datatwo}}
            <tr>
              <td>{{this.assetid}} </td>
              <td>{{this.assetName}}</td>
              <td>{{this.issueDate}}</td>
            </tr>
            {{/each}}
          </table>
        </div> --}}
      </div>
    </div>
  </div>

</body>

</html>